{
  "name": "server",
  "version": "1.0.0",
  "description": "Image Processing server",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "server": "nodemon server.js",
    "build": "npm install && npm install --prefix frontend && npm run build --prefix frontend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Sanket Jaswal",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "multer": "^1.4.5-lts.1",
    "nodemon": "^3.1.4",
    "sharp": "^0.33.5",
    "socket.io": "^4.7.5",
    "uuid": "^10.0.0"
  }
}
